import { FormatDocTypePipe } from './format-doc-type.pipe';

describe('FormatDocTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDocTypePipe();
    expect(pipe).toBeTruthy();
  });
});
